{
  "name": "task-analyzer",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "dev:web": "pnpm --filter @task-analyzer/web dev",
    "dev:server": "pnpm --filter @task-analyzer/server dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
